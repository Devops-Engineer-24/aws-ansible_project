plugin: amazon.aws.aws_ec2
regions:
  - ap-south-1
filters:
  tag:Environment: dev
  instance-state-name: running
hostnames:
  - private-ip-address
auth:
  aws_profile: dev_profile
keyed_groups:
  - key: tags.Name
